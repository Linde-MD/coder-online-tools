---
alwaysApply: false
description: 状态放置、共享方式、持久化和局部状态边界。新增或重构状态时读取。
---

# 状态管理

## 基础原则

- 局部状态优先，只有跨组件共享或持久化需要时才提升状态。
- 状态逻辑优先放 composables、services 或明确的 storage/infra 模块。

## 约束

- 不要把 UI 状态、业务状态和持久化状态混在一起。
- 不要使用难以追踪的隐式共享状态。

## 持久化

- 需要本地缓存时，应通过单独的存储封装完成，不要散落在组件内部。