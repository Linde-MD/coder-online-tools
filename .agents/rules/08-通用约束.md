---
alwaysApply: false
description: 通用约束，包括语言、注释、兜底行为和跨场景一致性。确认通用原则时读取。
---

# 通用约束

## 语言与表达

- 界面文案和必要注释使用中文。
- 代码命名保持英文且语义明确。

## 兜底与一致性

- 失败和空状态必须有清晰兜底，不要让用户面对无反馈状态。
- 不要在不同模块中重复定义冲突的约定。

## 非目标

- 不要为了局部开发便利破坏现有架构。
- 不要把临时实现写成新的默认标准。