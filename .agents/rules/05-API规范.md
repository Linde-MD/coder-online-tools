---
alwaysApply: false
description: 模块接口、数据边界、外部调用和错误处理规范。新增或修改接口时读取。
---

# API 规范

## 模块接口

- 模块对外暴露的函数、对象和返回值必须稳定、明确。
- 模块边界要显式，不要依赖隐式全局状态或魔法字段。

## 数据边界

- 请求、持久化、协议转换、数据适配应集中在 service、infra、domain 或 utils。
- UI 层不要直接承担数据解析、存储或协议细节。

## 错误处理

- 错误处理方式要统一，不要在不同层级重复实现同一种提示逻辑。
- 失败路径必须可预测，不能静默吞错。