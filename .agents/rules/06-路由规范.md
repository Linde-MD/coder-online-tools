---
alwaysApply: false
description: 路由结构、页面入口、导航关系与视图切换规范。新增页面或调整导航时读取。
---

# 路由规范

## 路由组织

- 路由入口统一管理，不要在多个位置重复定义同一路由语义。
- 页面级入口应保持薄编排，只负责路由壳和 feature 组合。

## 导航约束

- 路由和菜单、导航状态应保持一致，不要用 DOM 驱动页面切换。
- 活动视图状态应以路由或明确状态为准，不要混入临时全局变量。